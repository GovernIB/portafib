<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>es.caib.portafib</groupId>
  <artifactId>portafib-sqlgenerator</artifactId>
  <packaging>jar</packaging>

  <name>PortaFIB - Scripts - SQL Generator</name>

  <parent>
    <groupId>es.caib.portafib</groupId>
    <artifactId>portafib-scripts</artifactId>
    <version>2.0.5</version>
  </parent>


  <properties>	
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>

    <dependency>
      <groupId>es.caib.portafib</groupId>
      <artifactId>portafib-model</artifactId>
      <version>${project.version}</version>
      <type>jar</type>
      <scope>compile</scope>
    </dependency>

    <dependency>
      <groupId>es.caib.portafib</groupId>
      <artifactId>portafib-jpa</artifactId>
      <version>${project.version}</version>
      <type>jar</type>
      <scope>compile</scope>
    </dependency>

    <dependency>
      <groupId>es.caib.portafib</groupId>
      <artifactId>portafib-persistence-jta</artifactId>
      <version>${project.version}</version>
      <type>jar</type>
    </dependency>

    <dependency>
      <groupId>org.fundaciobit.genapp</groupId>
      <artifactId>genapp-gen</artifactId>
      <version>1.0</version>
    </dependency>

    <dependency>
      <groupId>javax.persistence</groupId>
      <artifactId>persistence-api</artifactId>
      <version>1.0</version>
    </dependency>

    <dependency>
      <groupId>org.hibernate</groupId>
      <artifactId>hibernate-core</artifactId>
      <version>3.3.2.GA</version>
    </dependency>

    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <version>1.2.14</version>
    </dependency>

    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>1.5.4</version>
    </dependency>

    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
      <version>1.5.4</version>			 
    </dependency>

  </dependencies>

  <build>

    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>1.3.2</version>
      </plugin>

      <!--
      Plugin per generar l'SQL per fer un drop de la base de dades
      Exemple:
       mvn -Dhibernate.dialect=org.hibernate.dialect.PostgreSQLDialect hibernate3:hbm2ddl@drop
      El fitxer resultant es troba dins target/sql/hibernate3/
      -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>hibernate3-maven-plugin</artifactId>
        <version>3.0</version>
        <executions>
          <execution>
            <id>drop</id>
            <configuration>
              <hibernatetool>
                <jpaconfiguration persistenceunit="portafibDB" />
                <hbm2ddl export="false" create="false" drop="true"
                         update="false" format="true" outputfilename="portafib_drop_schema.sql" />
              </hibernatetool>
            </configuration>
          </execution>
          <execution>
            <id>create</id>
            <configuration>
              <hibernatetool>
                <jpaconfiguration persistenceunit="portafibDB" />
                <hbm2ddl export="false" create="true" drop="false"
                         update="false" format="true" outputfilename="portafib_create_schema.sql" />
              </hibernatetool>
            </configuration>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

</project>
