<?xml version="1.0" encoding="UTF-8"?>
<server>

  <!-- ==================================================================== -->
  <!-- Mail Connection Factory                                              -->
  <!-- ==================================================================== -->

  <mbean code="org.jboss.mail.MailService" name="jboss:service=portafibMail">
    <attribute name="JNDIName">java:/es.caib.portafib.mail</attribute>
    <attribute name="Configuration">
      <configuration>
        <property name="mail.store.protocol" value="pop3"/>
        <property name="mail.transport.protocol" value="smtp"/>

        <property name="mail.user" value="nobody"/>
        <property name="mail.pop3.host" value="mail-service"/>
        <property name="mail.smtp.host" value="mail-service"/>
        <property name="mail.smtp.port" value="3025"/>

        <property name="mail.from" value="portafib@portafib.com"/>
        <property name="mail.debug" value="false"/>
      </configuration>
    </attribute>
    <depends>jboss:service=Naming</depends>
  </mbean>

</server>